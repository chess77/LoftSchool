script(type="text/x-template")#reviews-container

        .sliders-btn__box
            button.review-slider__btn.review-slider__btn--back.review-slider__btn--not-go(id="backBtn" @click="previous")
                +icon('arrow-down', 'btn__icon')
            button.review-slider__btn.review-slider__btn--next(id="nextBtn" @click="next")
                +icon('arrow-down', 'btn__icon')
        flickity(ref="flickity" :options="flickityOptions").reviews__box



                    ul.reviews__list(v-for="item in reviews" :key="item.name")


                            li.reviews__item.carousel-cell
                                .reviews__desc
                                    p {{item.text}}
                                .author__box
                                    .author__photo
                                        +image(`content/user.jpg`, "author__image")
                                    .author__info
                                        .author__name
                                            p ${{item.name}}
                                        .author__position
                                            p ${{item.position}}